# Deploy do backend no Render (free tier). O frontend vai na Vercel.
services:
  - type: web
    name: ekaizen-5s-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DB_PATH
        value: /tmp/ekaizen5s.db
      - key: CORS_ORIGINS
        sync: false # definir com a URL pública do frontend (Vercel)
