# Chave para assinar o token de sessão (gere uma forte em produção).
SECRET_KEY=dev-secret-troque-em-producao
# Caminho do arquivo SQLite.
DB_PATH=ekaizen5s.db
# Origens permitidas (CSV). Em produção, a URL pública do frontend.
CORS_ORIGINS=http://localhost:5173
